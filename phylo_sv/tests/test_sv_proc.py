import os
import csv
import unittest
import re
import numpy as np
import phylo_sv
from unittest import TestCase

class test_proc_svs(unittest.TestCase):
#    def test_is_same_sv(self):
#        self.assertTrue(phylo_sv.is_same_sv(x[0],y[0],ff))
